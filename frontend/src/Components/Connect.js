function Connect() {
  return (
    <button
      type="button"
      className="btn btn-outline-light me-2"
      // onClick={handleConnect}
    >
      Connect
    </button>
  );
}

export default Connect;
